{
  "name": "nextjs-sanity",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --no-sort --stream",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "workspaces": [
     "nextjs",
     "sanity-studio"
  ]
}
